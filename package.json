{
  "name": "h1-black-ui",
  "theme": "ui",
  "version": "0.0.0",
  "description": "Black backgrounds with colorful syntax",
  "keywords": [
    "dark",
    "black",
    "ui"
  ],
  "license": "MIT",
  "repository": "https://github.com/MarcosBessega/Glacier-Green-ui.git",
  "main": "lib/main",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  },
  "configSchema": {
    "fontSize": {
      "title": "Font Size",
      "description": "Atom UI forked for @berlouz glacier-dark-ui (https://atom.io/themes/glacier-dark-ui)",
      "type": [
        "integer",
        "string"
      ],
      "minimum": 8,
      "maximum": 20,
      "default": "Auto"
    },
    "layoutMode": {
      "title": "Layout Mode",
      "description": "In Auto mode, the UI will automatically adapt based on the window size.",
      "type": "string",
      "default": "Auto",
      "enum": [
        "Compact",
        "Auto",
        "Spacious"
      ]
    }
  }
}
